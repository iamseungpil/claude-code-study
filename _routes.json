{
  "version": 1,
  "description": "Route configuration for Cloudflare Pages - HTML files go through middleware",
  "include": [
    "/frontend/*.html"
  ],
  "exclude": [
    "/frontend/*.js",
    "/frontend/*.css",
    "/frontend/*.png",
    "/frontend/*.jpg",
    "/frontend/*.svg"
  ]
}
