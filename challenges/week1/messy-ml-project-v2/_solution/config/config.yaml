# Training Configuration
training:
  epochs: 10
  batch_size: 64
  learning_rate: 0.001
  seed: 42

model:
  num_classes: 10
  dropout_rate: 0.5

data:
  data_dir: ./data
  num_workers: 2

paths:
  checkpoint_dir: ./checkpoints
  log_dir: ./logs
